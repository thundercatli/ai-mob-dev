# Copy to keystore.properties (gitignored) to sign release builds with your own key.
# Without this file the release build falls back to Android's debug key, which is fine for
# local testing but not for distribution.
#
# storeFile is resolved relative to the repository root.
storeFile=release.jks
storePassword=changeit
keyAlias=release
keyPassword=changeit
